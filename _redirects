# Cloudflare Pages 重定向规则

# API 路由重定向到 Functions  
/api/* /api/:splat 200

# RSS 和 sitemap 处理
/rss.xml /rss.xml 200
/sitemap.xml /sitemap.xml 200

# 根路径直接指向中文首页
/ /zh/index.html 200

# SPA 路由回退 - 支持客户端路由
/* /index.html 200